# 蕾米埃尔 · Clawd 动画完善计划

本计划采用严格串行、阶段原子的实施方式。一个阶段只有在资产、配置、QA 和安装副本同步全部完成后，才允许进入下一阶段。每个阶段都保留独立输入、生成源、构建脚本与验收记录，确保中断后可以从最近一个已完成阶段继续，也可以只回滚当前阶段。

## 通用完成标准

每个阶段必须同时满足：

1. 新资产位于 `xingyu-clawd-theme/assets/`，生成源位于 `xingyu-clawd-theme/generated/<phase>/`。
2. `theme.json` 只引用已存在且通过检查的文件。
3. 生成独立 QA 拼图或动作采样图，并在 `xingyu-clawd-theme/qa/<phase>/` 保存验收记录。
4. 运行 Clawd 主题校验；所有配置引用和必需能力检查通过。
5. 将完成阶段同步到 `%APPDATA%\clawd-on-desk\themes\xingyu-clawd-theme\`。
6. 更新本文件中的状态、产物路径、验证结果和完成时间。
7. 当前阶段未通过时，不创建下一阶段的正式资产，也不修改下一阶段配置。

## 阶段状态

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| 1 | 重做移动动画与播放节奏 | 完成（2026-07-30） |
| 2 | 独立 `notification`、`sweeping`、`carrying` | 完成（2026-07-31） |
| 3 | `juggling`、`jugglingTiers`、`workingTiers` | 暂缓（用户指定） |
| 4 | 独立拖拽、左右双击、四击、annoyed 互动 | 暂缓（用户指定） |
| 5 | 随机待机与更新检查动画 | 完成（2026-07-31，一次生成直接接入） |
| 6 | SVG 鼠标视线跟随 | 暂缓（用户指定） |
| 7 | 完整 Mini Mode 动画组 | 未开始 |
| 8 | 全量回归验证与最终交付 | 未开始 |

## 阶段 1：重做移动动画与播放节奏

目标：用一个连续、可循环、稳定注册的右向移动动作替换当前顺序混乱的 16 帧动作；由同一已批准序列逐帧镜像得到左向拖拽版本。

验收标准：

- 右向源动作是一个连贯的 16 帧循环，不是 16 个独立姿势的拼接。
- 全程朝屏幕右侧移动，角色身份、设备、发饰、翅膀和比例一致。
- 每帧共享 360×360 透明画布、相同视觉尺度和稳定基线。
- 首尾可无明显跳变地循环。
- 不含速度线、尘土、阴影、拖影、文字或分离特效。
- 帧时长使用 95–120ms 的可读节奏，不再使用 65ms。
- `roam.gif` 与 `drag-right.gif` 使用通过 QA 的右向循环；`drag-left.gif` 使用逐帧镜像版本。
- 本阶段不再改写双击动作；双击独立化留到阶段 4。
- 产出动作采样图、逐帧接触表、帧尺寸/数量/时长检查和视觉验收记录。

阶段产物：

- `generated/phase-01-movement/`
- `assets/roam.gif`
- `assets/drag-right.gif`
- `assets/drag-left.gif`
- `qa/phase-01-movement/`

完成记录：

- 使用连贯的右向 4×4、16 帧源序列；左向版本由已批准右向帧逐帧镜像得到。
- 最终帧规格为 360×360、每帧 110ms、无限循环，总循环 1760ms。
- 统一中心与基线；全部帧无画布边缘接触。
- GIF 二值透明前执行轮廓绿/青色去污染，并在黑底、灰底复检通过。
- `roam.gif` 与 `drag-right.gif` SHA-256：`9b60e3517868c01aa729c216250bb28d8a2039e5a98524b04d6f9e62f468f4a1`。
- `drag-left.gif` SHA-256：`62848dd93bc0a23c59ec7cad2c74ca1b43a962ed28f656c2e50baa33c4301e4e`。
- 仓库主题与安装主题对应文件哈希一致；18 个主题资产引用全部存在。
- 独立视觉 QA 最终结论：通过。

## 阶段 2：notification、sweeping、carrying

三个状态分别生成并验收，任一失败只重做该状态：

- `notification`：明确的请求批准/等待用户输入动作。
- `sweeping`：整理桌面、资料或设备的清扫/归整动作。
- `carrying`：抱着或搬运与角色设备风格一致的物品。

完成后才将三个状态同时接入 `theme.json`。

完成记录：

- `notification.gif`：16 帧、130ms/帧；独立的批准/输入提醒动作。
- `sweeping.gif`：16 帧、140ms/帧；经过道具时序返修后，完整表现散卡归拢、送入设备和循环恢复。
- `carrying.gif`：16 帧、140ms/帧；经过取放过渡返修后，完整表现抓取、抬起、负重搬运与放下。
- 三个动作均通过黑底/灰底透明边缘检查与独立视觉 QA。
- 三项全部通过后才一次性接入 `theme.json`；主题版本更新为 `0.4.0`。
- 仓库与安装主题的配置和三个动画哈希一致；20 个主题资产引用全部存在。

## 阶段 3：子代理与多会话工作分级

- 单子代理：较轻松、协作感明确的动作。
- 2+ 子代理：明显更忙碌的 juggling 动作。
- 单会话、双会话、3+ 会话分别使用不同强度的工作动作。
- 接入 `states.juggling`、`jugglingTiers` 和 `workingTiers`。

## 阶段 4：鼠标互动独立化

分别制作并接入：

- 拖拽通用、向左、向右。
- 左双击、右双击。
- 连续四击。
- annoyed。

不得再让双击文件与拖拽/漫游文件哈希相同。

## 阶段 5：随机待机与更新检查

- 至少两个随机待机动作，例如张望和阅读/检查设备。
- 独立的更新检查动作。
- 接入 `idleAnimations` 与 `updateVisuals.checking`。
- 按用户要求采用一次生成直接接入；不进行主观打回和二次生成，只检查文件、帧数、时长与配置引用能否运行。

完成记录：

- `idle-look.gif`：16 帧、170ms/帧，通过 `idleAnimations` 随机播放 6500ms。
- `idle-reading.gif`：16 帧、170ms/帧，通过 `idleAnimations` 随机播放 10000ms。
- `update-checking.gif`：16 帧、140ms/帧，通过 `updateVisuals.checking` 接入。
- 三项均采用一次实际生成直接构建，没有进行画面打回或二次生成。
- 仅执行机械检查：GIF 可解码、16 帧、360×360、无限循环、时长正确，23 个主题资产引用均存在。
- 主题版本更新为 `0.5.0`，仓库和安装主题对应文件哈希一致。

## 阶段 6：SVG 鼠标视线跟随

- 制作外部主题可用、无脚本的 SVG。
- 包含 `eyes-js`，并在适合时加入 `body-js`。
- 接入并验证 `eyeTracking.states: ["idle"]`。

## 阶段 7：Mini Mode

先完成官方要求的 8 个基础动作：

- `mini-idle`
- `mini-enter`
- `mini-peek`
- `mini-alert`
- `mini-happy`
- `mini-sleep`
- `mini-crabwalk`
- `mini-enter-sleep`

基础动作全部通过后再添加可选 `mini-working`，最后启用 `miniMode.supported`。

## 阶段 8：全量回归

- 运行完整主题校验。
- 检查所有引用资产、帧数、画布、循环、命中框和状态回退。
- 生成最终主题接触表与动作索引。
- 同步安装目录并记录最终版本号。

## 恢复规则

中断后先查看本文件的阶段状态，再查看对应 `qa/<phase>/status.json`：

- `complete`：保持该阶段不变，从下一阶段开始。
- `in_progress`：只检查和继续当前阶段。
- `failed`：只修复当前阶段列出的失败资产。
- 没有状态文件：视为阶段未开始。
